#!/usr/bin/env ruby

require "bundler/inline"
gemfile do
  source "https://rubygems.org"
  gem "multi_repo", require: "multi_repo", path: File.expand_path("~/dev/multi_repo")
end

require "irb"
IRB.start
